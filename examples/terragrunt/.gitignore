# Terragrunt / OpenTofu working artifacts — never committed.
.terragrunt-cache/
# gruntcmt plan output dir (terragrunt --out-dir/--json-out-dir target).
out/
*.tfplan
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
*.bak
